# brand_assets/

Drop here:
- `logo.png` (transparent background, vector if available)
- `palette.json` or `palette.txt` — HEX codes
- Font files OR `fonts.txt` listing names if licensed
- Brand guidelines PDF if one exists
- `design_system.json` — copy from `/SHARED/03_templates/design-system-template.json` and fill in client values

These assets are read by Agent 9 (Brand Translator) when producing JSON prompts.
